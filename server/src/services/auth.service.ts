type User = {
    username?: string,
    email?: string,
    password?: string,
}

class AuthService {
    async signUpService(user: User) {

    }

    async loginService(user: User) {
        
    }
}